package edu.pingpong.quickstart;

import io.quarkus.test.junit.NativeImageTest;

@NativeImageTest
public class NativeGreetigsResourceIT extends GreetigsResourceTest {

    // Execute the same tests but in native mode.
}